/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['png.pngtree.com', 'freepngimg.com', 'res.cloudinary.com', 'upload.wikimedia.org'],
  },
};

module.exports = nextConfig;
