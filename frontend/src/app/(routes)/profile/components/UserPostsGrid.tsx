import React from 'react';

const UserPostsGrid: React.FC = () => {
  return (
    <div className="grid grid-cols-3 gap-4 p-4">
      <div className="bg-red-200">Map through user posts and display them in each grid item</div>
      <div className="bg-red-200">Map through user posts and display them in each grid item</div>
      <div className="bg-red-200">Map through user posts and display them in each grid item</div>
      <div className="bg-red-200">Map through user posts and display them in each grid item</div>
      <div className="bg-red-200">Map through user posts and display them in each grid item</div>
      <div className="bg-red-200">Map through user posts and display them in each grid item</div>
      <div className="bg-red-200">Map through user posts and display them in each grid item</div>
      <div className="bg-red-200">Map through user posts and display them in each grid item</div>
    </div>
  );
};

export default UserPostsGrid;
